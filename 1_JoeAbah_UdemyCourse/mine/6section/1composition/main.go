package main

//composition is a design principle in which a class is composed of one or more objects of other classes, rather than inheriting from them. This allows for greater flexibility and modularity in the design of the class, as it can use the functionality of the composed objects without being tightly coupled to their implementation. In Go, composition is often achieved through the use of struct embedding, where one struct can embed another struct to gain access to its fields and methods.
